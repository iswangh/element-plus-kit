# @iswangh/element-plus-kit 开发文档

本文档详细解释了 `@iswangh/element-plus-kit` 包的完整开发配置，包括 `package.json`、`tsconfig.json` 和 `vite.config.ts` 的详细说明。

---

## 📋 目录

- [包定位说明](#包定位说明)
- [package.json 配置详解](#packagejson-配置详解)
- [tsconfig.json 配置详解](#tsconfigjson-配置详解)
- [vite.config.ts 配置详解](#viteconfigts-配置详解)
- [配置文件之间的关系](#配置文件之间的关系)
- [开发工作流](#开发工作流)
- [常见问题](#常见问题)

---

## 包定位说明

### 🎯 Kit 包的定位

`@iswangh/element-plus-kit` 是一个**聚合包（Aggregation Package）**，其主要职责是：

1. **统一入口**：提供统一的包入口，用户只需安装一个包即可使用所有功能
2. **重新导出**：重新导出各个子包（如 `form`、`core`）的组件和类型
3. **工具函数**：提供全局安装函数和 Resolver 配置工具
4. **最小化源代码**：包内只包含必要的导出逻辑，不包含业务组件源代码

### ⚠️ 重要说明

**Kit 包不应该包含业务组件的源代码**：

- ✅ **应该包含**：
  - 导出逻辑（`src/index.ts`）
  - 安装函数（`src/utils/install.ts`）
  - Resolver 配置（`src/utils/resolver.ts`）
  - 类型重新导出（`src/types/index.d.ts`、`src/types/form.d.ts`）

- ❌ **不应该包含**：
  - Vue 组件源代码（`.vue` 文件）
  - 业务逻辑代码
  - 样式文件

**原因**：
- 保持包结构清晰，职责单一
- 业务组件应该在对应的子包中（如 `form` 包）
- 便于维护和扩展

---

## package.json 配置详解

### 📋 完整配置（带注释说明）

```json
{
  // 包的唯一标识符，使用 scoped package 命名（@iswangh/ 前缀），避免命名冲突
  // 这是主包名称，不包含子包标识（如 /form、/core）
  // 用户安装此包后，可以导入所有子包的功能
  "name": "@iswangh/element-plus-kit",
  
  // 指定包使用 ES Module（ESM）格式，而不是 CommonJS（CJS）
  // 使用 ES Module 语法（import/export），现代 JavaScript 标准，更好的 Tree Shaking 支持
  // 配置一致性：与 vite.config.ts 的 formats: ['es'] 和 tsconfig.json 的 module: "ESNext" 保持一致
  "type": "module",
  
  // 包的版本号，遵循语义化版本（SemVer）规范：主版本号.次版本号.修订号
  // 0.1.0：主版本号（0）= 不兼容的 API 修改，次版本号（1）= 向下兼容的功能性新增，修订号（0）= 向下兼容的问题修正
  "version": "0.1.0",
  
  // 包的简短描述，用于 npm 搜索和展示（建议不超过 160 个字符）
  "description": "Element Plus Kit - 基于 Element Plus 的 Vue 3 组件集合",
  
  // 包的作者或组织名称
  "author": "iswangh",
  
  // 包的许可证类型（Apache-2.0 是常用的开源许可证，提供专利保护）
  // 允许商业使用、修改、分发，并提供专利授权保护
  "license": "Apache-2.0",
  
  // 代码仓库地址，方便用户查看源代码和提交问题
  "repository": {
    "type": "git",
    "url": "https://gitee.com/iswangh/element-plus-kit.git"
  },
  
  // 包的关键词，用于 npm 搜索和分类（建议使用 5-10 个关键词）
  "keywords": [
    "vue",
    "vue3",
    "element-plus",
    "components",
    "form",
    "ui"
  ],
  
  // 定义包的导出入口，这是现代 npm 包的标准配置方式（Node.js 12+ 支持）
  // 更精确的控制、更好的安全性、支持条件导出、TypeScript 支持、多入口点支持
  // 配置一致性说明：
  //   - 输出目录：所有导出路径都指向 dist/ 目录，与 tsconfig.json 的 outDir 和 vite.config.ts 的默认输出目录一致
  //   - 多入口点：支持主入口（index）和 resolver 入口，与 vite.config.ts 的 build.lib.entry 配置保持一致
  //   - 类型定义：每个入口都提供对应的类型定义文件，与 tsconfig.json 的 declaration: true 配置一致
  "exports": {
    // 主入口点，对应 import pkg from '@iswangh/element-plus-kit'
    // types: TypeScript 类型定义文件路径（与 tsconfig.json 的 outDir 一致）
    // import: ES Module 导入时的入口文件（与 vite.config.ts 的 build.lib.entry.index 一致）
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    // Resolver 入口点，对应 import { ElementPlusKitResolver } from '@iswangh/element-plus-kit/resolver'
    // 用于按需导入配置，提供独立的入口，方便用户按需使用
    // 与 vite.config.ts 的 build.lib.entry.resolver 配置保持一致
    "./resolver": {
      "types": "./dist/resolver.d.ts",
      "import": "./dist/resolver.js"
    }
  },
  
  // 向后兼容旧版本的 Node.js 和构建工具
  // main: CommonJS 和旧版 Node.js 的入口点
  // module: 为支持 ESM 的打包器提供 ES Module 入口
  // types: TypeScript 类型定义文件的路径
  // 优先级：支持 exports 的环境优先使用 exports，不支持的环境回退到 main、module、types
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  
  // 发布到 npm 时包含的文件列表
  "files": [
    "README.md",
    "dist"
  ],
  
  // 脚本命令配置
  "scripts": {
    // 构建生产版本：使用 Vite 进行构建，输出到 dist/ 目录，生成优化后的代码和类型定义文件
    "build": "vite build",
    
    // 开发模式构建：监听文件变化自动重新构建，适用于开发时测试构建输出
    "dev": "vite build --watch",
    
    // 类型检查：使用 vue-tsc 检查所有 TypeScript 文件，不生成输出文件，用于 CI/CD 中验证类型正确性
    "type-check": "vue-tsc --noEmit",
    
    // 清理构建输出目录：删除 dist/ 目录及其所有内容，使用跨平台的 Node.js 命令，兼容 Windows、macOS、Linux
    "clean": "node -e \"require('fs').rmSync('dist', {recursive: true, force: true})\"",
    
    // 发布前的自动检查脚本：在发布到 npm 之前自动执行，确保构建和类型检查都通过
    // 如果构建或类型检查失败，发布会被阻止，保证发布到 npm 的包质量
    "prepublishOnly": "pnpm build && pnpm type-check"
  },
  
  // 对等依赖：使用此包的项目必须安装的依赖
  // 不自动安装：npm/pnpm 不会自动安装 peer dependencies
  // 版本要求：
  //   - element-plus: "^2.11.7" 表示兼容 2.11.7 及以上、3.0.0 以下的版本
  //   - vue: "^3.5.23" 表示兼容 3.5.23 及以上、4.0.0 以下的版本
  // 避免重复安装：确保项目中只有一个 Vue 和 Element Plus 实例
  // 为什么使用 peer dependencies：
  //   - 避免版本冲突：确保使用项目中的 Vue 和 Element Plus 版本
  //   - 减小包体积：不将 Vue 和 Element Plus 打包进库中
  //   - 灵活性：允许用户选择 Vue 和 Element Plus 版本（在兼容范围内）
  // 配置一致性：与 vite.config.ts 的 rollupOptions.external 配置保持一致，这些依赖不会被打包进库中
  "peerDependencies": {
    "element-plus": "^2.11.7",
    "vue": "^3.5.23"
  },
  
  // 运行时依赖：会被打包进库中
  // workspace:*：Monorepo 工作区协议，表示使用工作区内的 form 包
  // 此依赖会被打包进最终的库文件中，用户安装 kit 包时，form 包也会被安装
  // 为什么使用 workspace 协议：
  //   - 开发时使用源码：开发时直接使用工作区内的源码
  //   - 发布时自动解析：发布到 npm 时，pnpm 会自动解析为正确的版本号
  //   - 避免版本冲突：确保使用工作区内的版本
  // 注意：kit 包依赖 form 包，但不直接依赖 core 包（因为 form 包已经依赖了 core 包）
  // 配置一致性：form 包和 core 包在 vite.config.ts 的 rollupOptions.external 中也被标记为外部依赖，不会被打包进库中
  "dependencies": {
    "@iswangh/element-plus-kit-form": "workspace:*"
  },
  
  // 开发时依赖：只在开发、构建、测试时需要，不会被打包发布
  "devDependencies": {
    // Vite 的 Vue 插件：用于处理 .vue 文件（虽然 kit 包不包含 Vue 组件，但可能需要处理类型），构建时可能需要
    "@vitejs/plugin-vue": "^6.0.1",
    
    // TypeScript 编译器：类型检查、编译 TypeScript 代码
    "typescript": "^5.9.2",
    
    // 自动导入插件：用于生成自动导入配置，kit 包提供的 ElementPlusKitResolver 同时支持 AutoImport 和 Components
    "unplugin-auto-import": "^20.1.0",
    
    // Vue 组件自动导入插件：用于生成组件自动导入配置，kit 包提供 ElementPlusKitResolver 需要此依赖
    "unplugin-vue-components": "^30.0.0",
    
    // 现代前端构建工具：构建、打包、开发服务器
    "vite": "^7.1.5",
    
    // Vite 的类型定义生成插件：自动生成 TypeScript 类型定义文件（.d.ts），支持多入口点的类型声明合并，确保发布的包包含完整的类型支持
    "vite-plugin-dts": "^4.5.4",
    
    // Vue 3 框架（开发版本）：开发时测试和类型定义
    // 注意：element-plus 和 vue 在 devDependencies 中是开发版本，实际运行时使用 peerDependencies 中的版本
    "vue": "^3.5.23",
    
    // Vue 3 的 TypeScript 类型检查工具：检查 .vue 文件中的 TypeScript 类型，生成类型定义文件（.d.ts）
    "vue-tsc": "^3.0.7"
  },
  
  // 发布配置：指定发布到 npm 时的访问权限
  "publishConfig": {
    "access": "public"
  }
}
```

---

## tsconfig.json 配置详解

### 📋 完整配置

```json
{
  // 继承根目录的基础 TypeScript 配置
  "extends": "../../tsconfig.app.json",
  "compilerOptions": {
    // 启用复合项目模式，支持 TypeScript Project References
    "composite": true,
    // 增量编译信息文件路径
    "tsBuildInfoFile": "../../node_modules/.tmp/packages-kit.tsbuildinfo",
    // 类型库：ES2022 和 DOM API
    "lib": ["ES2022", "DOM"],
    // 基础路径：当前包目录
    "baseUrl": ".",
    // 根目录：src 目录
    "rootDir": "./src",
    // 模块系统：ESNext（ES 模块）
    "module": "ESNext",
    // 模块解析策略：使用 bundler 模式
    "moduleResolution": "bundler",
    // 路径别名：清空继承的 paths，强制使用包名导入
    // 配置一致性：与 vite.config.ts 不配置 resolve.alias 保持一致，统一使用包名导入，pnpm workspace 自动解析到工作区内的源码
    "paths": {},
    // 启用 package.json exports 字段解析
    "resolvePackageJsonExports": true,
    // 包含的全局类型定义：Node.js 类型（用于构建工具）
    "types": ["node"],
    // 禁止导入 .ts 扩展名（由构建工具处理）
    "allowImportingTsExtensions": false,
    // 生成类型声明文件（.d.ts）
    "declaration": true,
    // 生成类型声明文件的 source map
    "declarationMap": true,
    // 允许输出文件（构建时需要）
    "noEmit": false,
    // 输出目录：dist
    // 配置一致性：与 package.json 的 exports 路径和 vite.config.ts 的默认输出目录保持一致
    "outDir": "./dist",
    // 跳过类型库的类型检查（提升编译速度）
    "skipLibCheck": true
  },
  // 项目引用：依赖 form 包（通过 TypeScript Project References 管理类型依赖）
  "references": [
    {
      "path": "../form",
      // 不前置输出：kit 包不包含 form 包的代码
      "prepend": false
    }
  ],
  // 包含的文件：src 目录下的所有文件
  "include": ["src/**/*"],
  // 排除的文件：node_modules 和 dist 目录
  "exclude": ["node_modules", "dist"]
}
```

---

## vite.config.ts 配置详解

### 📋 完整配置

```typescript
// 路径解析工具：在 ES Module 环境中获取当前文件目录
import { resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
// Vue 插件：处理 .vue 文件（虽然 kit 包不包含 Vue 组件，但保留以确保兼容性）
import vue from '@vitejs/plugin-vue'
// Vite 配置定义函数：提供类型提示和配置验证
import { defineConfig } from 'vite'
// TypeScript 类型定义生成插件：自动生成 .d.ts 文件
import dts from 'vite-plugin-dts'

// 获取当前文件目录（ES Module 环境中的 __dirname 替代方案）
const __dirname = fileURLToPath(new URL('.', import.meta.url))

/**
 * Kit 包构建配置
 *
 * 主包，聚合所有组件和工具，提供统一的入口
 */
export default defineConfig({
  plugins: [
    // Vue 单文件组件支持
    vue(),
    // 类型声明文件生成插件
    dts({
      // 包含的文件：所有 src 目录下的文件
      include: ['src/**/*'],
      // 排除的文件：测试文件
      exclude: ['src/**/*.test.ts', 'src/**/*.spec.ts'],
      // 输出目录：dist
      outDir: 'dist',
      // 复制类型声明文件到输出目录
      copyDtsFiles: true,
      // 合并类型声明文件：将多个入口的类型声明合并
      rollupTypes: true,
      // 日志级别：静默（不输出日志）
      logLevel: 'silent',
    }),
  ],
  build: {
    // 库模式配置
    lib: {
      // 多入口配置：支持多个入口点（index 和 resolver）
      // 配置一致性：与 package.json 的 exports 配置保持一致，支持主入口（"."）和 resolver 入口（"./resolver"）
      entry: {
        // 主入口：组件库主入口（对应 package.json 的 exports["."]）
        index: resolve(__dirname, 'src/index.ts'),
        // resolver 入口：用于 unplugin-vue-components 的自动导入解析器（对应 package.json 的 exports["./resolver"]）
        resolver: resolve(__dirname, 'src/utils/resolver.ts'),
      },
      // 库名称（用于 UMD 格式，当前仅使用 ES 格式）
      name: 'ElementPlusKit',
      // 输出文件名：根据入口名称生成（index.js 和 resolver.js）
      fileName: (format, entryName) => `${entryName}.js`,
      // 输出格式：仅 ES 模块
      formats: ['es'],
    },
    rollupOptions: {
      // 外部依赖：不打包到库中，由使用者提供（避免重复打包，减小库体积）
      // 配置一致性：与 package.json 的 peerDependencies 和 dependencies 保持一致
      //   - vue 和 element-plus：在 peerDependencies 中声明，由使用者提供
      //   - @iswangh/element-plus-kit-form 和 @iswangh/element-plus-kit-core：在 dependencies 中声明，但标记为外部依赖，运行时通过包管理器解析
      external: [
        'vue',
        'element-plus',
        '@iswangh/element-plus-kit-form',
        '@iswangh/element-plus-kit-core',
      ],
      output: {
        // 全局变量映射（用于 UMD 格式，当前未使用）
        globals: {
          'vue': 'Vue',
          'element-plus': 'ElementPlus',
        },
      },
    },
  },
})
```

**注意**：kit 包不配置 `resolve.alias`，使用包名导入即可。pnpm workspace 会自动将包名（`@iswangh/element-plus-kit-form`）解析到工作区内的源码，Vite 原生支持通过 `package.json` 的 `exports` 字段解析包名。

---

## 配置文件之间的关系

### 🔗 配置关联图

```
package.json
    │
    ├─ type: "module" ──────────────┐
    │                                │
    ├─ exports.import ───────────────┤
    │                                │
    ├─ exports["./resolver"] ────────┤
    │                                │
    └─ scripts.build ─────────────────┤
                                     │
vite.config.ts ──────────────────────┼─ 构建配置
    │                                │
    ├─ build.lib.entry (多入口) ────┘
    │
    ├─ build.rollupOptions.external ─┐
    │                                 │
    └─ resolve.alias (已移除) ────────┤
                                      │
tsconfig.json ───────────────────────┼─ 类型配置
    │                                 │
    ├─ outDir: "./dist" ──────────────┘
    │
    ├─ references ───────────────────┐
    │                                 │
    └─ declaration: true ─────────────┤
                                      │
package.json ──────────────────────────┘
    │
    ├─ exports.types: "./dist/index.d.ts"
    └─ exports["./resolver"].types: "./dist/resolver.d.ts"
```


## 开发工作流

### 🚀 开发流程

#### 1. 开发阶段

```bash
# 启动监听模式，自动重新构建
npm run dev
```

**流程**：
1. 修改 `src/` 目录下的源代码（主要是导出逻辑）
2. Vite 自动检测变化并重新构建
3. 输出到 `dist/` 目录
4. 其他包可以立即使用更新后的代码

**注意**：kit 包的源代码主要是导出逻辑，不包含业务组件。

---

#### 2. 类型检查

```bash
# 检查类型错误
npm run type-check
```

**流程**：
1. 使用 `vue-tsc` 检查所有 TypeScript 文件
2. 不生成输出文件（`--noEmit`）
3. 发现类型错误时停止并报告

**使用场景**：
- 提交代码前检查类型错误
- CI/CD 流程中的类型验证
- 确保代码质量

---

#### 3. 构建生产版本

```bash
# 构建生产版本
npm run build
```

**流程**：
1. Vite 使用 Rollup 进行构建
2. TypeScript 编译生成 JavaScript 和类型定义
3. 生成多个入口点（`index.js` 和 `resolver.js`）
4. 输出到 `dist/` 目录
5. 准备发布到 npm

**输出文件**：
- `dist/index.js`：主入口 JavaScript 文件
- `dist/index.d.ts`：主入口类型定义文件
- `dist/resolver.js`：Resolver 入口 JavaScript 文件
- `dist/resolver.d.ts`：Resolver 入口类型定义文件

---

#### 4. 清理构建产物

```bash
# 清理 dist 目录
npm run clean
```

**流程**：
1. 删除 `dist/` 目录
2. 清理所有构建产物
3. 准备重新构建

---

### 📦 发布流程

**重要**：在 Monorepo 项目中，应使用根目录的发布命令，而不是直接在包目录中执行 `npm publish`。

#### 发布

发布前会自动检查当前 Git 分支（通过 Changesets pre 钩子），只能在 `main` 或 `master` 分支上发布：

使用根目录的发布命令（**推荐**）：

```bash
# 从项目根目录执行（使用 Changesets 自动管理版本和发布）
pnpm release
```

**自动执行流程**：
1. **分支检查**：通过 Changesets pre 钩子检查当前分支是否为 `main` 或 `master`
2. **发布**：按依赖顺序发布包（core → form → kit）
3. **构建和类型检查**：每个包的 `prepublishOnly` 钩子自动执行构建和类型检查
4. **发布到 npm**：如果所有检查通过，自动发布到 npm

**注意**：版本更新应在发布前通过 `pnpm version` 命令完成。

**手动执行**（不推荐，仅用于测试）：

```bash
# 进入包目录
cd packages/kit

# 手动构建
pnpm build

# 手动类型检查
pnpm type-check

# 直接发布（不推荐，会跳过分支检查）
npm publish
```

**发布内容**：
- `dist/` 目录（构建产物）
- `README.md`（文档）
- `package.json`（配置）

---

## 常见问题

### Q1: 为什么 kit 包需要 Vue 插件，但不包含 Vue 组件？

**A**: 
- kit 包虽然不包含 Vue 组件源代码，但可能需要处理类型定义
- 某些情况下，Vue 插件有助于正确处理类型
- 保留此配置以确保兼容性和类型处理的正确性

---

### Q2: 为什么 `external` 中包含 `@iswangh/element-plus-kit-form` 和 `@iswangh/element-plus-kit-core`？

**A**: 
- 这些包会在运行时通过包管理器（pnpm）解析
- 不需要打包进 kit 包中，减小包体积
- 确保使用正确的包版本

---

### Q3: 为什么需要多入口点配置？

**A**: 
- **主入口（`index`）**：提供完整的包功能
- **Resolver 入口（`resolver`）**：提供按需导入配置
- 用户可以根据需要选择导入的内容，减小最终打包体积

---

### Q4: 为什么不需要配置 `paths` 和 `alias`？

**A**: 
- **使用包名导入**：在 Monorepo 中，应该使用包名（`@iswangh/element-plus-kit-form`）而不是路径别名
- **pnpm workspace 自动解析**：pnpm workspace 会自动将包名解析到工作区内的源码
- **Vite 原生支持**：Vite 原生支持通过 `package.json` 的 `exports` 字段解析包名
- **简化配置**：不需要维护路径别名配置，减少配置复杂度
- **与发布后一致**：开发环境和发布后的导入方式完全一致

---

### Q5: Kit 包应该包含哪些源代码？

**A**: 

**应该包含**：
- ✅ 导出逻辑（`src/index.ts`）：重新导出各个子包的功能
- ✅ 安装函数（`src/utils/install.ts`）：全局安装函数
- ✅ Resolver 配置（`src/utils/resolver.ts`）：自动导入配置工具（同时支持 unplugin-vue-components 和 unplugin-auto-import）
- ✅ 类型重新导出（`src/types/index.d.ts`、`src/types/form.d.ts`）：类型定义重新导出

**不应该包含**：
- ❌ Vue 组件源代码（`.vue` 文件）：应该在对应的子包中（如 `form` 包）
- ❌ 业务逻辑代码：应该在对应的子包中
- ❌ 样式文件：应该在对应的子包中

**原因**：
- 保持包结构清晰，职责单一
- 业务组件应该在对应的子包中
- 便于维护和扩展

---

### Q6: 为什么执行 `pnpm install` 后，kit 包中会出现 `form` 或 `core` 目录？

**A**: 

这是**符号链接（Symbolic Link）**，可能是由以下原因自动创建的：

1. **TypeScript Project References**：某些工具或 IDE 在处理 `tsconfig.json` 的 `references` 配置时可能创建符号链接
2. **IDE 行为**：部分 IDE（如 VS Code）在处理 TypeScript Project References 时可能创建符号链接
3. **开发工具**：某些开发工具为了支持类型解析而创建符号链接

**重要说明**：
- ✅ **不会影响发布**：`package.json` 的 `files` 字段只包含 `["README.md", "dist"]`，符号链接不会被包含在发布包中
- ✅ **已在 `.gitignore` 中**：已添加规则忽略这些符号链接，不会提交到 Git
- ✅ **可以安全删除**：如果符号链接出现，可以安全删除，不会影响功能

**如何处理**：
1. **忽略它**：由于已在 `.gitignore` 中，Git 会忽略它，不会影响发布
2. **手动删除**（如果需要）：
   ```bash
   # Windows
   rmdir /s /q packages\kit\form
   
   # macOS/Linux
   rm -rf packages/kit/form
   ```

**正确的依赖位置**：
- ✅ `packages/kit/node_modules/@iswangh/element-plus-kit-form` - 这是 pnpm workspace 的正确符号链接位置
- ❌ `packages/kit/form` - 这是开发环境的符号链接，可以忽略

---

### Q7: 如何添加新的子包导出？

**A**: 

1. **修改 `src/index.ts`**：
```typescript
// 导出新的子包
export { NewComponent } from '@iswangh/element-plus-kit/new-package'
export type { NewType } from '@iswangh/element-plus-kit/new-package'
```

2. **修改 `package.json`**：
```json
{
  "dependencies": {
    "@iswangh/element-plus-kit-form": "workspace:*",
    "@iswangh/element-plus-kit/new-package": "workspace:*"
  }
}
```

3. **修改 `vite.config.ts`**：
```typescript
rollupOptions: {
  external: [
    // ... 其他依赖
    '@iswangh/element-plus-kit/new-package',
  ],
}
```

---

## 📚 相关资源

- [npm package.json 官方文档](https://docs.npmjs.com/cli/v9/configuring-npm/package-json)
- [TypeScript 配置文档](https://www.typescriptlang.org/tsconfig)
- [Vite 构建配置](https://vitejs.dev/config/)
- [Vue 3 文档](https://vuejs.org/)
- [Element Plus 文档](https://element-plus.org/)
- [pnpm workspace 文档](https://pnpm.io/workspaces)

---

## 📄 更新日志

- **2024-XX-XX**：初始版本，包含完整开发配置说明
- **2024-XX-XX**：更新 Vue 版本到 3.5.23，Element Plus 版本到 2.11.7
- **2024-XX-XX**：移除路径别名配置，统一使用包名导入

---

**文档维护者**：Element Plus Kit 团队  
**最后更新**：2024年
