# Changesets 配置详解

## 📋 配置文件位置

`.changeset/config.json`

## 🔧 配置项详解

### 1. `$schema`

```json
"$schema": "https://unpkg.com/@changesets/config@3.1.1/schema.json"
```

**作用**：
- 指定配置文件的 JSON Schema 地址
- 提供编辑器验证和自动补全功能
- 帮助发现配置错误和提供类型提示

**说明**：
- 这是标准的 JSON Schema 特性
- 版本号（`3.1.1`）对应 `@changesets/config` 包的版本
- 编辑器（如 VSCode）会自动下载 Schema 进行验证

---

### 2. `changelog`

```json
"changelog": "@changesets/cli/changelog"
```

**作用**：
- 定义如何生成变更日志（CHANGELOG.md）
- 指定变更日志生成器的路径或模块

**可选值**：
- `"@changesets/cli/changelog"`：使用 Changesets 默认的变更日志生成器（推荐）
- `false`：不生成变更日志
- 自定义模块路径：使用自定义的变更日志生成器

**说明**：
- 默认生成器会为每个包生成独立的 CHANGELOG.md
- 格式遵循 [Keep a Changelog](https://keepachangelog.com/) 规范
- 变更日志会包含版本号、日期和变更描述

**示例输出**：
```markdown
# @iswangh/element-plus-kit-core

## 0.1.3

### Patch Changes

- 修复了表单验证逻辑的 bug
```

---

### 3. `commit`

```json
"commit": false
```

**作用**：
- 控制 `changeset version` 命令是否自动创建 Git 提交

**可选值**：
- `false`：不自动提交（推荐，手动控制）
- `true`：自动提交版本更新和 CHANGELOG

**推荐设置**：`false`

**原因**：
- 手动控制提交时机和提交信息
- 可以自定义提交信息格式
- 符合项目的提交规范（Conventional Commits）

**使用场景**：
- `false`：适合需要自定义提交信息的项目
- `true`：适合完全自动化的 CI/CD 流程

---

### 4. `fixed`

```json
"fixed": []
```

**作用**：
- 声明哪些包应该共享相同的版本号（固定版本策略）
- 这些包会始终使用相同的版本号

**格式**：
```json
"fixed": [
  ["@iswangh/element-plus-kit-core", "@iswangh/element-plus-kit-form"]
]
```

**使用场景**：
- 包之间紧密耦合，需要同步版本
- 例如：UI 组件库和其主题包

**当前项目**：
- 使用独立版本策略（`[]`）
- 每个包独立管理版本号（core、form、kit 可以有不同的版本）

---

### 5. `linked`

```json
"linked": []
```

**作用**：
- 声明哪些包应该共享相同的版本号（链接版本策略）
- 与 `fixed` 类似，但语义略有不同

**格式**：
```json
"linked": [
  ["@iswangh/element-plus-kit-core", "@iswangh/element-plus-kit-form", "@iswangh/element-plus-kit"]
]
```

**与 `fixed` 的区别**：
- `fixed`：强制同步版本，即使只有一个包有变更
- `linked`：只有当所有包都有变更时才同步版本

**使用场景**：
- 包之间相对独立，但希望保持版本一致
- 例如：工具库的不同模块

**当前项目**：
- 使用独立版本策略（`[]`）
- 每个包独立管理版本号

---

### 6. `access`

```json
"access": "public"
```

**作用**：
- 定义包的发布访问级别
- 控制包发布到 npm 时的可见性

**可选值**：
- `"public"`：公开发布，任何人都可以安装（推荐用于开源项目）
- `"restricted"`：受限发布，需要付费或特殊权限（用于私有包）

**说明**：
- 对于作用域包（`@iswangh/element-plus-kit-core`），默认是 `restricted`
- 需要设置为 `"public"` 才能公开发布
- 或者在 `package.json` 中设置 `"publishConfig": { "access": "public" }`

**当前项目**：
- 所有包都是公开的，设置为 `"public"`

---

### 7. `baseBranch`

```json
"baseBranch": "main"
```

**作用**：
- 指定用于比较变更的基础分支
- 用于检测哪些包发生了变化

**可选值**：
- `"main"`：主分支（推荐）
- `"master"`：主分支（旧命名）
- 其他分支名

**使用场景**：
- `changeset status` 命令会比较当前分支与 `baseBranch` 的差异
- 用于检测哪些包需要发布

**当前项目**：
- 使用 `"main"` 作为主分支

---

### 8. `updateInternalDependencies`

```json
"updateInternalDependencies": "patch"
```

**作用**：
- 设置当内部依赖（workspace 包）发生变化时，如何更新依赖包的版本
- 控制内部依赖的版本更新策略

**可选值**：
- `"patch"`：总是更新为 patch 版本（推荐）
- `"minor"`：总是更新为 minor 版本
- `"major"`：总是更新为 major 版本

**示例**：
假设 `@iswangh/element-plus-kit-core` 从 `0.1.2` 更新到 `0.2.0`（minor 版本）：

- `"patch"`：依赖它的 `@iswangh/element-plus-kit-form` 会更新为 `0.1.3`（patch）
- `"minor"`：依赖它的 `@iswangh/element-plus-kit-form` 会更新为 `0.2.0`（minor）
- `"major"`：依赖它的 `@iswangh/element-plus-kit-form` 会更新为 `1.0.0`（major）

**推荐设置**：`"patch"`

**原因**：
- 避免不必要的版本跳跃
- 保持依赖版本的稳定性
- 符合语义化版本规范

**当前项目**：
- 使用 `"patch"`，确保内部依赖更新时不会过度提升版本

---

### 9. `ignore`

```json
"ignore": []
```

**作用**：
- 指定在版本更新时应忽略的包
- 这些包不会参与版本更新和发布流程

**格式**：
```json
"ignore": [
  "playground",
  "docs",
  "@iswangh/element-plus-kit-playground"
]
```

**使用场景**：
- 开发工具包（如 playground、docs）
- 测试包
- 不发布的包

**注意事项**：
- 包名必须与 `package.json` 中的 `name` 字段完全匹配
- 或者使用 glob 模式（如 `"playground-*"`）
- 如果包是 `private: true`，Changesets 会自动忽略，无需在此列出

**当前项目**：
- `playground` 和 `docs` 都是 `private: true`，Changesets 会自动忽略
- 所以 `ignore` 数组为空 `[]`

---

## 📊 配置总结

| 配置项 | 当前值 | 说明 |
|--------|-------|------|
| `$schema` | `"https://unpkg.com/@changesets/config@3.1.1/schema.json"` | JSON Schema 验证 |
| `changelog` | `"@changesets/cli/changelog"` | 使用默认变更日志生成器 |
| `commit` | `false` | 不自动提交，手动控制 |
| `fixed` | `[]` | 独立版本策略 |
| `linked` | `[]` | 独立版本策略 |
| `access` | `"public"` | 公开发布 |
| `baseBranch` | `"main"` | 主分支 |
| `updateInternalDependencies` | `"patch"` | 内部依赖更新策略 |
| `ignore` | `[]` | 不忽略任何包（private 包自动忽略） |

## 🔄 版本策略对比

### 独立版本（当前项目）

```json
{
  "fixed": [],
  "linked": []
}
```

**特点**：
- 每个包独立管理版本号
- core、form、kit 可以有不同版本
- 适合包之间相对独立的场景

**示例**：
- `@iswangh/element-plus-kit-core`: `0.1.2`
- `@iswangh/element-plus-kit-form`: `0.1.3`
- `@iswangh/element-plus-kit`: `0.2.0`

### 统一版本（备选方案）

```json
{
  "linked": [
    ["@iswangh/element-plus-kit-core", "@iswangh/element-plus-kit-form", "@iswangh/element-plus-kit"]
  ]
}
```

**特点**：
- 所有包共享版本号
- 适合包之间紧密耦合的场景

**示例**：
- `@iswangh/element-plus-kit-core`: `0.1.2`
- `@iswangh/element-plus-kit-form`: `0.1.2`
- `@iswangh/element-plus-kit`: `0.1.2`

## 📚 更多资源

- [Changesets 使用指南](./index.md)
- [Changesets 官方文档](https://github.com/changesets/changesets)
- [配置选项参考](https://github.com/changesets/changesets/blob/main/docs/config-file-options.md)
